<h1>Listing students</h1>

<table>
  <thead>
    <tr>
      <th>First</th>
      <th>Last</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @students.each do |student| %>
      <div id="student" class="<%= student.id %>">  
        <tr>
          <td><%= student.first %></td>
          <td><%= student.last %></td>
          <td><%= student.email %></td>
          <td><%= link_to 'Show', student %></td>
          <td><%= link_to 'Edit', edit_student_path(student) %></td>
          <td><%= link_to 'Destroy', student, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      </div> 
    <% end %>
  </tbody>
</table>

<br>



<%= link_to 'New Student', new_student_path %>


$(document).ready(function){
  var set = 2;

  if $("#student").hasClass(>set) {
    $().hide();
  }
};

# class based on variable id
# cycling
# showing them in pairs
# revealing

JS
jQuery